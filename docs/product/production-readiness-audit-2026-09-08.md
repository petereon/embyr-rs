# Production-Readiness Audit — 2026-09-08

Full-codebase scan, run after `known-gaps.md`'s own 8-item list was fully closed. Dispatched
7 parallel domain-specific subagents (security, SRE/reliability, database, Rust code quality,
DevOps/deployment, dependency supply-chain, over-engineering/bloat) plus orchestrator synthesis
and cross-corroboration. Read-only — no code changed as part of the scan itself. Status column
updated as each item is picked up, same convention as `known-gaps.md`.

| # | Category | Finding | Location | Severity | Status |
|---|---|---|---|---|---|
| 1 | Security | Stripe webhook signing secret defaults to empty string when unset; webhook route stays mounted, HMAC becomes forgeable (empty key is a valid HMAC key). Unauthenticated attacker can set any tenant's subscription status/plan. | `crates/embyr-server/src/main.rs:216` (`.unwrap_or_default()`); `crates/embyr-server/src/admin/router.rs:400-406` (unconditional mount); `crates/embyr-server/src/admin/handlers/webhooks_stripe.rs:129,174,231,264` | **Blocker** | **CLOSED** 2026-09-09 — see `docs/evolution/2026-09-08-stripe-webhook-secret-required.md`. Server now refuses to start when Stripe billing is configured without a webhook signing secret; the webhook route is not mounted at all when Stripe is fully unconfigured |
| 2 | Security | Unbounded Prometheus label cardinality driven by an unauthenticated, unvalidated `project_id` — rate limiter runs *before* `authenticate()`, `extract_project_id` accepts any non-empty string, registry has no eviction. Unauthenticated remote OOM. | `crates/embyr-server/src/middleware/rate_limit.rs:143-148`; `crates/embyr-server/src/grpc/handler.rs:99-107,1267,1272`; `crates/embyr-server/src/observability.rs:39-47` | **Blocker** | **CLOSED** 2026-09-09 — see docs/evolution/2026-09-09-rate-limiter-project-id-validation.md |
| 3 | Security | Unbounded request-body buffering on the unauthenticated Stripe webhook route (`to_bytes(body, usize::MAX)`) — signature not checked until after the full body is in memory, no body-limit layer anywhere in the crate. | `crates/embyr-server/src/admin/middleware/stripe_signature.rs:44` | **Blocker** | **CLOSED** 2026-09-09 — see docs/evolution/2026-09-09-stripe-webhook-body-limit.md |
| 4 | Reliability / DB | Real-time LISTEN/NOTIFY has no reconnect logic and the dead-listener map entry is never evicted — one transient Postgres blip permanently and silently kills realtime delivery for that project for the life of the process, while its dedicated connection pool leaks forever. Confirmed independently by 2 agents (SRE + database). | `crates/embyr-server/src/adapters/postgres_notify_listener.rs:57-72` (recv-error → `break`, no restart); `crates/embyr-server/src/grpc/handler.rs:3547-3565` (`contains_key`-only guard, no `.remove()` anywhere in the workspace) | **Blocker** | **CLOSED** 2026-09-09 — see docs/evolution/2026-09-09-realtime-listener-reconnect.md |
| 5 | Database | Composite-index feature never creates a real Postgres index — `IndexManager`/`create_composite_index` only read/write a metadata row (`status='ready'` set on INSERT, no build step). Zero GIN/expression index exists on the `fields` JSONB column; every "index ready" filtered/sorted query is actually a full sequential scan. | `crates/embyr-server/src/adapters/index_manager.rs:19-31`; `crates/embyr-server/src/admin/handlers/composite_indexes.rs:118-123`; `migrations/customer/0001_documents.sql:12-14` (only real index, a partial btree with no JSONB coverage) | **Blocker** | **CLOSED** 2026-09-09 — see docs/evolution/2026-09-09-composite-index-real-creation.md |
| 6 | Database | Documented 168h soft-delete purge sweeper does not exist. `deleted_at` is write-only (set once on soft-delete, never read anywhere else) — soft-deleted projects, including ECIES-encrypted customer DSNs/credentials, persist forever, contradicting an explicit admin-UI promise. | `crates/embyr-server/src/admin/handlers/lifecycle.rs:181` (sets, never read elsewhere — confirmed via full-repo grep); `crates/embyr-admin-ui/src/views/db_detail/mod.rs:282` (UI promise) | **Blocker** | **CLOSED** 2026-09-09 — see docs/evolution/2026-09-09-soft-delete-purge-sweeper.md |
| 7 | Reliability | `aws_secret`/`gcp_secret` backend modes are hardcoded to `None` fetchers in the real composition root — every request for a project in that mode returns `Status::internal`. Admin provisioning API still allows creating such projects. Pre-existing, named in ADR-054 §D7, but still blocks launch for that backend mode. | `crates/embyr-server/src/main.rs:164-165,239-246`; `crates/embyr-server/src/grpc/handler.rs:268-271,286-289` | **Blocker** | **CLOSED** 2026-09-09 — see docs/evolution/2026-09-09-wire-secret-fetchers.md |
| 8 | DevOps | No build/release path exists for `embyr-agent` at all — no Dockerfile, no CI build target, nothing in `.github/workflows/ci.yml` references it. The customer-VPC agent (the product's core privacy boundary) cannot currently be distributed to a customer. | Root-level: absence confirmed via `find`/CI config read | **Blocker** | **CLOSED** 2026-09-09 — see docs/evolution/2026-09-09-embyr-agent-release-pipeline.md — ALL 8 BLOCKERS NOW CLOSED |
| 9 | Rust / Correctness | Client-controlled OCC precondition timestamp panics the write path — `Precondition.update_time.{seconds,nanos}` flows unvalidated from the client into a `.expect("valid timestamp")` on out-of-range/malformed input, dropping the RPC ungracefully instead of a clean `invalid_argument`. | `crates/embyr-pg-storage/src/backend_adapter.rs:209-213` (`to_datetime`), called at `:398,:1051`; `crates/embyr-server/src/grpc/handler.rs:527-529` (`convert_precondition`, no validation) | High | **CLOSED** 2026-09-10 — see docs/evolution/2026-09-10-occ-precondition-validation.md |
| 10 | Security / Correctness | Raw Postgres/sqlx driver error text is returned to the client verbatim as the gRPC status message on every backend failure (52+ call sites) — can leak column/constraint/schema detail. | `crates/embyr-server/src/grpc/handler.rs:4148-4162` (`core_error_to_status` catch-all `Status::internal(e.to_string())`) | High | **CLOSED** 2026-09-12 — see docs/evolution/2026-09-12-sanitize-backend-error-messages.md |
| 11 | Security | `embyr-agent`'s own field-path validator only rejects consecutive dots — doesn't call `embyr_core::validate_field_path` (the real charset guard the server uses). Feeds the same raw-interpolating SQL builder. SQL injection reachable via a client-cert holder into the *customer's own* Postgres, defeating the agent's entire reason to exist (vendor cannot read customer data). mTLS-gated, so not remote-unauthenticated. | `crates/embyr-agent/src/server.rs:117-126,149`; sink at `crates/embyr-pg-storage/src/encoding/query.rs:326` and siblings | High | **CLOSED** 2026-09-12 — see docs/evolution/2026-09-12-agent-field-path-validation.md |
| 12 | Security | Argon2id (64 MiB/t=3/p=4) runs inline on tokio async worker threads (no `spawn_blocking` anywhere) on the admin signin route, which has zero rate-limit middleware and a password-failure lockout that never increments (only TOTP failures do). Unlimited unthrottled password guessing + ~100 concurrent requests ≈ 6GB and reactor starvation. | `crates/embyr-server/src/admin/handlers/auth.rs:166-197,281-301`; `crates/embyr-server/src/admin/router.rs:186-190` (`public_router`, no middleware) | High | **CLOSED** 2026-09-13 — see docs/evolution/2026-09-13-admin-signin-hardening.md |
| 13 | Security | Admin signin leaks valid account emails via a timing oracle — unknown email returns instantly, known email takes ~50-100ms (full Argon2id verify) despite an identical response *shape*. Combined with #12's missing rate limit, cheaply enumerates the admin user list. | `crates/embyr-server/src/admin/handlers/auth.rs:151,182,192` | High | **CLOSED** (bounded, not eliminated) 2026-09-13 — see docs/evolution/2026-09-13-admin-signin-hardening.md |
| 14 | Reliability | Pre-authentication 3-round-trip amplification against the shared system DB — an unknown `project_id` triggers UPDATE + SELECT EXISTS + INSERT before `authenticate()` ever runs, on the same pool auth/sessions/admin API depend on. | `crates/embyr-server/src/middleware/rate_limit.rs:203-255` | High | **CLOSED** 2026-09-13 — see docs/evolution/2026-09-13-preauth-db-amplification.md. **Note**: a valid-charset-but-unprovisioned `project_id` still costs 3 round trips — documented residual, now tracked separately as finding #49, not silently left off this row |
| 15 | DevOps / SRE | `/healthz` is a hardcoded `200 OK` with zero dependency checks — no liveness/readiness split. An orchestrator has no way to detect a pod whose Postgres connection died mid-flight. Confirmed independently by 2 agents (DevOps + SRE). | `crates/embyr-server/src/grpc/healthz.rs:4-6`, mounted on both :8081 and :9090 | High | **CLOSED** 2026-09-14 — see docs/evolution/2026-09-14-healthz-dependency-checks.md |
| 16 | Reliability | Postgres pool sizing is hardcoded tiny (`max_connections(5)` or `(2)`) everywhere with no env override, and most pools have no `acquire_timeout` (sqlx 30s default) — saturated tenants see 30s tail latency before a clean error instead of a fast fail. No concurrency limiter exists anywhere to shed load before it queues. | `crates/embyr-server/src/adapters/system_db.rs:230-233`; `crates/embyr-pg-storage/src/backend_adapter.rs:42-46`; `crates/embyr-server/src/grpc/handler.rs:3552-3556`; `crates/embyr-db-prep/src/main.rs:69-70` | High | **CLOSED** 2026-09-14 — see docs/evolution/2026-09-14-pool-sizing-and-limits.md |
| 17 | Database | Collection-group queries (`all_descendants=true`) build a leading-`%` `LIKE` that can't use the `(project_id, collection_path)` btree — every collection-group query scans the entire project, not just the target collection group. Compounds #5. | `crates/embyr-pg-storage/src/backend_adapter.rs:575-577` (mirrored in `run_aggregation_query`) | High | **CLOSED** 2026-09-14 — see docs/evolution/2026-09-14-collection-group-query-index.md |
| 18 | Database / DevOps | No backup/disaster-recovery documentation exists anywhere in the repo — RTO/RPO, PITR guarantees, and backup ownership (embyr's vs. customer's for BYOC/`direct_pg`) are entirely unstated. | `docs/` — absence confirmed via full-text search | High | **CLOSED** 2026-09-15 — see docs/evolution/2026-09-15-backup-disaster-recovery-docs.md |
| 19 | DevOps | No deployment automation beyond the Dockerfile itself (no K8s manifests, Helm chart, ECS task def, or docker-compose), and no release process — crate version frozen at `0.1.0`, no CHANGELOG, no git tags. "The release" is whatever commit is on `master` when someone runs `docker build`. | Repo-wide absence, confirmed via `find`/`git tag -l` | High | **CLOSED** (K8s/Helm/ECS explicitly deferred to a future `production-hosting-topology` finding) 2026-09-15 — see docs/evolution/2026-09-15-deployment-release-process.md |
| 20 | Security | Rate limiter fails open on database errors and conflates "DB error" with "allowed" (`.ok().flatten()` discards the error, `.unwrap_or(false)` on the exists-check). An attacker who degrades the system DB (see #14) turns off rate limiting globally as a side effect. | `crates/embyr-server/src/middleware/rate_limit.rs:171-181,217,232-238,250` | Medium | **CLOSED** 2026-09-15 — see docs/evolution/2026-09-15-rate-limiter-fail-open.md |
| 21 | Security | No CORS layer exists on :8081 despite it serving gRPC-Web/BrowserChannel. Fails closed today (no active exploit), but no origin policy has been written or reviewed — landmine for whoever adds `CorsLayer::permissive()` under deadline pressure. | Repo-wide grep for `CorsLayer`/`allow_origin`: zero hits in `crates/` | Medium | **CLOSED** 2026-09-15 — see docs/evolution/2026-09-15-cors-origin-policy.md |
| 22 | Security / Ops | TLS is entirely opt-in with no startup warning when it's off; all 3 listeners bind `0.0.0.0` including admin :9090. Wiring is correct when configured (verified: rustls 0.23 safe defaults, TLS1.2+/AEAD only) — gap is purely that nothing nudges an operator to turn it on. | `crates/embyr-server/src/config.rs:122`; `crates/embyr-server/src/main.rs:51-52,260-265,82,90,185` (warns for other things, not this) | Medium | **CLOSED** 2026-09-15 — see docs/evolution/2026-09-15-tls-startup-warning.md |
| 23 | Dependencies | `cargo deny` (the `cargo audit`-equivalent advisory check) only runs on push/PR to master, never on a schedule — a newly published RUSTSEC advisory against an unchanged dependency is invisible until someone happens to push. | `.github/workflows/ci.yml` | Medium | **CLOSED** 2026-09-15 — see docs/evolution/2026-09-15-scheduled-cargo-deny.md |
| 24 | Reliability | No explicit message/request-body size limits configured anywhere (zero hits for `max_decoding_message_size`/`DefaultBodyLimit`/etc.) — relying entirely on tonic/axum's implicit library defaults rather than a deliberate, tested ceiling. | `crates/embyr-server/src/lib.rs:348,447-448` | Medium | **CLOSED** 2026-09-19 — see docs/evolution/2026-09-19-request-body-size-limits.md |
| 25 | Reliability | Logging is plain-text (`tracing_subscriber::fmt()`), not structured JSON — worse parsing reliability for production log aggregators (Loki/CloudWatch/Datadog) at scale. | `crates/embyr-server/src/main.rs:73-79` | Medium | **CLOSED** 2026-09-19 — see docs/evolution/2026-09-19-structured-json-logging.md |
| 26 | Security / DB | Server-side field-path SQL interpolation (`format!`, never `push_bind`) is injection-safe *only* because every entry point is funneled through one central `validate_field_path` gate — zero defense-in-depth if a future caller (new RPC, internal tool, test harness) constructs a filter without going through `translate_filter`. Directly demonstrated as a real gap by #11 (the agent's own divergent, weaker copy). | `crates/embyr-pg-storage/src/encoding/query.rs` (`append_field_filter` etc.); `crates/embyr-core/src/domain/query.rs:16-21` (the one gate) | Medium | **CLOSED** 2026-09-19 — see docs/evolution/2026-09-19-field-path-defense-in-depth.md |
| 27 | Dependencies | Duplicate dependency versions in the graph (testcontainers 0.21/0.23, tower 0.4/0.5, base64 0.21/0.22, thiserror 1/2) — binary-bloat and patch-fragmentation risk (a CVE on the old version's path may not be caught by scanning only the explicit workspace pin). Mostly dev-only/transitive. | `cargo tree --duplicates` output | Medium | **CLOSED** 2026-09-19 — see docs/evolution/2026-09-19-dependency-version-dedup.md |
| 28 | DevOps | Env-var documentation is scattered across 3 separate config structs' own rustdoc (`ServerConfig`, `AgentConfig`, `DbPrepConfig`) with no root README or `.env.example` cross-linking them. | Repo-wide: no `README*`/`.env.example` found outside `.git/hooks/README.sample` | Medium | **CLOSED** 2026-09-19 — see docs/evolution/2026-09-19-env-var-documentation.md |
| 29 | DevOps | No Grafana dashboard, alert rules, or runbook checked in despite Prometheus metrics existing — an operator builds all of this from scratch, including for the Postgres-down-after-startup scenario in #15. | Repo-wide: no `*runbook*`/`*grafana*`/`*dashboard*`/`*alert*` found outside unrelated admin-UI view names | Medium | **CLOSED** 2026-09-19 — see docs/evolution/2026-09-19-observability-runbook.md — LAST Medium finding; all Medium items (#20-29) now closed alongside the already-closed High arc (#9-19) |
| 30 | Database | Postgres pool sizing has no env override anywhere (`ServerConfig` reads only `DATABASE_URL`), and no documentation ties replica count to Postgres's own `max_connections`. Same underlying issue as #16, DB-specific angle. | `crates/embyr-server/src/adapters/system_db.rs:230-232`; `crates/embyr-pg-storage/src/backend_adapter.rs:42-44` | Medium | **CLOSED** 2026-09-14 — see docs/evolution/2026-09-14-pool-sizing-and-limits.md |
| 31 | Security | Resume token's 32-byte BLAKE3 suffix is unkeyed and never actually verified — `decode_ts` only reads the first 8 bytes. Decorative; impact contained since the stream is already authenticated to one project (worst case: self-inflicted large-scan amplification via a forged old timestamp). | `crates/embyr-server/src/realtime/resume_token.rs:11-33` | Low | **CLOSED** 2026-09-19 — chose option (b): removed the unused BLAKE3 suffix (dead code) rather than adding keyed verification, since the query's project scope always comes from the request's own authenticated target (never the token), so no cross-project replay path exists — see commit `fa3db3f` |
| 32 | Security | Admin signin re-derives Argon2 params inline (`Argon2::new(...)`) instead of calling the shared `argon2_instance()`/`verify_password` — functionally identical today, but bypasses the one test (`hash_password_uses_identical_argon2id_parameters_to_hash_api_key`) written specifically to prevent drift. | `crates/embyr-server/src/admin/handlers/auth.rs:187-191`; shared source of truth at `crates/embyr-core/src/auth/argon2.rs:9-15` | Low | **CLOSED** 2026-09-19 — already fixed by commit 5a54e22 (admin-signin-hardening, #12/#13); signin now calls `embyr_core::auth::argon2::verify_password` via `spawn_blocking`, no inline `Argon2::new(...)` remains; verified no code change needed |
| 33 | Security | ECIES doesn't bind the ephemeral or recipient public key into the KDF (raw DH output only) — standard practice mixes both in for domain separation against key-substitution. Not currently exploitable given the single-fixed-recipient design; worth an explicit key-compromise runbook regardless, since an API-key compromise retroactively decrypts every stored backend DSN by design. | `crates/embyr-core/src/auth/ecies.rs:35` (KDF); `:12-17` (static-secret-from-API-key design) | Low | **CLOSED** 2026-09-19 — HKDF `info` now binds `ephemeral_pubkey \|\| recipient_pubkey`; no persisted ciphertext in this repo (all encrypt/decrypt call sites are same-run round-trips) so no re-encryption/migration concern here — production-provisioned data IS affected, see docs/evolution/2026-09-19-ecies-kdf-domain-separation.md |
| 34 | DevOps | A live-looking Stripe test secret key sits unencrypted in `.env.local` on disk — correctly gitignored and confirmed never committed, but one careless `git add -A` away from landing in a commit. | `.env.local:1` | Low | **CLOSED** 2026-09-19 — verified safe, no code change needed: `.env.local` confirmed in `.gitignore`, `git log --all --full-history -- .env.local` returns empty (never committed) |
| 35 | DevOps | CI's full-workspace test job (~38 binaries) has already hit runner disk-space limits once (documented in the workflow's own comments, `ld` Bus errors from truncated mmap output) and needed a workaround. Evidence the full-suite run is close to the runner's resource ceiling as test count keeps growing. | `.github/workflows/ci.yml:61-69` | Low | **CLOSED** 2026-09-19 — verified the "Free disk space" workaround (removing unused preinstalled SDKs, `docker image prune`) is still present and its explanatory comment still accurate; no further action needed, informational finding only |
| 36 | Rust | `cargo clippy --workspace --all-targets` currently fails outright (exit 101) on `#[deny(clippy::option_env_unwrap)]` in test-only harness code — blocks the CI gate if clippy is meant to block merges. Test-only, zero production-binary risk, mechanical fix (`env!` instead of `option_env!().expect()`). | `tests/customer_db_onboarding/.../common/mod.rs:196,310` | Low | **CLOSED** 2026-09-19 — `env!` swap rejected: this file is `#[path]`-shared into two different crates' `[[test]]` targets and `CARGO_BIN_EXE_*` is only set for the owning package, so `env!` would hard-fail compilation for the other package; fixed with `#[allow(clippy::option_env_unwrap)]` + comment at both sites instead |
| 37 | Rust | `PostgresBackendAdapter` (1389 lines — every document CRUD/query/OCC/transaction operation) has zero inline unit tests; correctness rests entirely on Docker-backed acceptance suites. Would have caught #9's panic in milliseconds instead of needing a live DB run. | `crates/embyr-pg-storage/src/backend_adapter.rs` | Low | **CLOSED** 2026-09-19 — 4 representative unit tests added for the pure, DB-free logic: `cursor_operator` (ASC/DESC cursor-operator flip, 8-case table) and `uuid_from_bytes` (16-byte length validation, valid + 5 invalid lengths). `to_datetime` and `index_build_succeeded` already had dedicated unit tests from prior features (#9, collection-group-query-index) — not duplicated. Rest of the file is thin SQL-construction-and-execute tied to `self.pool`, not independently pure. |
| 38 | Dependencies | `async-stripe`/`-core`/`-billing`/`-webhook` are pinned to `1.0.0-rc.8` — a pre-1.0 release candidate powering the real payment/billing integration. Consciously documented as accepted risk; worth a pre-launch check for whether stable 1.0 has since shipped. | Root `Cargo.toml:83-113` | Low | **CLOSED** 2026-09-19 — checked crates.io: latest is `1.0.0-rc.9`, stable 1.0 has not shipped yet; pin left as-is, no version bump made |
| 39 | Security | Attacker-controlled `event_type` becomes a Prometheus label on the webhook route — same unbounded-cardinality mechanism as #2, but gated behind Stripe signature verification (compounds #1 rather than standing alone). | `crates/embyr-server/src/admin/handlers/webhooks_stripe.rs:78` | Low | **CLOSED** 2026-09-19 — reused #2's sentinel-collapse pattern: `bounded_event_type_label()` passes through the 4 handled event types, collapses everything else to `"other"` — see commit `fa3db3f` |
| 40 | Rust | 4x `clippy::too_many_arguments` in the security-rules CEL evaluator (8-9 args, several same-typed `BTreeMap` params) — real transposition-bug risk given these functions recurse into each other on every rules-gated request. | `crates/embyr-core/src/access_control/mod.rs:1194,1268,1455,1514` | Low | **CLOSED** 2026-09-19 — bundled the 7 shared params into a named `EvalContext<'a>` struct threaded through `evaluate`/`eval_bool`/`compare_operands`/`resolve_field_value` and all ~55 call sites (including the ~17 production call sites in embyr-server); named fields make a `resource_fields`/`request_resource_fields` transposition a compile error instead of a silent swap |
| 41 | Rust | 2x `clippy::sliced_string_as_bytes` in the rules-file path parser — allocation/clarity nit, zero correctness impact. | `crates/embyr-core/src/access_control/rules_file.rs:666,679` | Low | **CLOSED** 2026-09-19 — applied clippy's own suggested fix (`s[idx..].as_bytes()` → `s.as_bytes()[idx..]`) at both sites; 141/141 access_control tests pass, lint clean |

## Bloat / Over-Engineering (separate pass — correctness, security, and performance explicitly out of scope for this axis)

| Tag | Finding | Location | Status |
|---|---|---|---|
| `delete:` | Entire `embyr-admin` crate is a dead 6-line stub (prints a startup message, does nothing) — the real admin server lives in `embyr-server`. Not built by CI. 10 unused dependencies. | `crates/embyr-admin/` | **REMOVED** 2026-09-19 — re-verified zero crate dependents and not in CI before deleting; `static/*.jsx` (orphaned pre-Leptos React frontend, ~2900 lines) removed with it |
| `delete:` | `MetricsPort` trait — zero implementations, zero usages anywhere. | `crates/embyr-core/src/storage/metrics_port.rs` | **REMOVED** 2026-09-19 |
| `delete:` | `NotifyPort` trait — zero implementations, self-admitted "placeholder" in its own doc comment. | `crates/embyr-core/src/storage/notify_port.rs` | **REMOVED** 2026-09-19 |
| `delete:` | `FakeEmailSender` dead test scaffold — doesn't even implement `IEmailSender`, never constructed anywhere, comment confirms "not wired." | `tests/admin_api_v2/common/mod.rs:491-517` | **REMOVED** 2026-09-19 — distinct from `tests/client_auth_hosted_identity/common/mod.rs::FakeEmailSender`, which does implement `IEmailSender` and is live; that one was left untouched |
| `delete:` | `SmtpEmailSender::new_scaffold()` — only panics, zero callers. Re-add when the V2 SMTP slice ships. | `crates/embyr-server/src/adapters/email.rs:11-17` | **REMOVED** 2026-09-19 — V2 SMTP slice still unshipped, still zero callers |
| `delete:` | `crate::transactions::occ` re-export shim — zero callers (real OCC verification is called directly). | `crates/embyr-server/src/transactions/occ.rs` | **REMOVED** 2026-09-19 — `transactions` module had no other contents, so the whole module (not just the shim) was deleted |
| `delete:` | `crate::encoding::query` re-export shim — zero callers. | `crates/embyr-server/src/encoding/query.rs` | **REMOVED** 2026-09-19 |
| `delete:` | `http-body` workspace dependency declared, never imported (only `http_body_util` is used, a different crate). | `crates/embyr-server/Cargo.toml`, root `Cargo.toml` | **REMOVED** 2026-09-19 |
| `yagni:` | `IQueryLogWriter` port has exactly one implementation, no test fake — mitigated by an explicit ADR-013 rationale, lower-confidence finding, not urging removal. | `crates/embyr-core/src/admin/query_log.rs` | **REVIEWED, KEPT** 2026-09-19 — per the audit's own explicit non-recommendation |

`net: -3438 lines (3441 removed / 3 added), -1 dep (http-body), -1 crate (embyr-admin).` (actual, from `git diff --stat` on commit 7e0d848; supersedes the original `-90 lines` estimate, which undercounted the `embyr-admin/static/*.jsx` frontend files)

## Follow-Up Scan — 2026-09-19

Run after every item above (all 41 numbered findings + the bloat pass) was CLOSED. Dispatched
6 parallel domain-specific subagents (security, reliability/SRE, database, Rust code quality,
DevOps/dependencies/bloat, deployment-readiness) plus a dedicated deployment-readiness gap
analysis requested separately by the user ("what's missing to actually deploy this"). Read-only —
no code changed as part of the scan itself. Each agent was told to skip anything already CLOSED
above and focus on genuinely new gaps, including anything introduced by findings #31-41's own
fixes earlier the same day.

| # | Category | Finding | Location | Severity | Status |
|---|---|---|---|---|---|
| 42 | DevOps / Deployment | No container image registry publishing exists anywhere in CI — both `embyr-server` and `embyr-agent` images are built and tagged (`:ci`) in the `docker`/`agent` CI jobs, then discarded. Nothing downstream of "merge to master" is pullable by anything, K8s or not, SaaS or customer-VPC. Root cause of #43. | `.github/workflows/ci.yml` `docker` job (~line 149) and `agent` job (~line 218) — both `docker build` only, zero `docker push`, no registry (ghcr.io/ECR/Docker Hub) configured anywhere in the repo | **Blocker** | **CLOSED** 2026-09-19 — see docs/evolution/2026-09-19-container-registry-publishing.md |
| 43 | DevOps / Deployment | `embyr-agent`'s "release pipeline" (closed as Blocker #8 on 2026-09-09) isn't actually customer-consumable today — its output is a GitHub Actions artifact (repo-access-gated, ~90-day retention) plus a Docker image built-and-tagged in CI only, never pushed. Same root cause as #42; a real customer has no documented way to obtain the binary/image. | CI `agent` job (comment there incorrectly attributes registry publishing to finding #19's scope, which never built it); `docs/product/journeys/agent-deployment.yaml`'s `kubectl apply` step assumes a pullable image that doesn't exist | **Blocker** | **CLOSED** 2026-09-19 — same fix as #42 (ghcr.io push); `agent-deployment.yaml` itself has no literal `image:` field to update (narrative journey doc, not a real K8s manifest) — see docs/evolution/2026-09-19-container-registry-publishing.md |
| 44 | Database / Correctness | Cursor pagination (`startAt`/`startAfter`/`endAt`/`endBefore`) silently drops the WHERE-clause boundary entirely for any cursor field type other than Integer/String/Double — including Timestamp, the single most common real-world `orderBy`+cursor pairing. No upstream validation rejects the combination. Result: silent wrong data (every page restarts from the beginning), not a crash, on ordinary usage. Predates this whole session's work and the original 2026-09-08 audit; never previously caught. | `crates/embyr-pg-storage/src/backend_adapter.rs:895-917` (startAt/startAfter), `:934-956` (endAt/endBefore), both `match &cursor.values[0] { Integer(..)\|String(..)\|Double(..) => ..., _ => {} }`; no type check in `crates/embyr-server/src/grpc/handler.rs:3092-3108` | **High** | **CLOSED** 2026-09-19 — both cursor blocks now reuse `push_scalar_comparison` (Integer/String/Double/Boolean/Timestamp/Bytes/Reference); Array/Map/Null rejected upstream in `handler.rs` — see docs/evolution/2026-09-19-timestamp-cursor-pagination.md |
| 45 | DevOps / Deployment | No reference single-host/non-K8s production deployment path exists — `docker-compose.yml` is explicitly dev-only per its own header, and the only other guidance is one throwaway `docker run embyr-server` line in ADR-017 with no restart-policy, log-rotation, reverse-proxy, or TLS-termination guidance for an operator not adopting K8s. | `docker-compose.yml`; `docs/product/architecture/adr-017-production-startup.md:323`; `docs/operations/release-process.md`'s own rollback procedure says "deploy the rebuilt image in place" without saying how | **High** | **CLOSED** 2026-09-19 — see `docs/operations/single-host-deployment.md` and `docs/evolution/2026-09-19-deployment-operator-runbooks.md` |
| 46 | DevOps / Deployment | IAM/secret-creation steps for AWS/GCP secrets-manager sourcing (`EMBYR_*_AWS_SECRET_ARN`/`*_GCP_SECRET_NAME`) are undocumented — ADR-018 documents the code's fetch behavior but no operator runbook gives the actual IAM policy JSON, secret naming/ARN format, or GCP-equivalent permissions needed before it will work. | `docs/product/architecture/adr-018-secrets-management.md`; no corresponding `docs/operations/` runbook step | **High** | **CLOSED** 2026-09-19 — see `docs/operations/secrets-manager-setup.md` and `docs/evolution/2026-09-19-deployment-operator-runbooks.md` |
| 47 | Reliability / SRE | 4 background sweepers (`cap_usage_refresher`, `transaction_sweeper`, `soft_delete_purge_sweeper` — the one purging sensitive credential columns — and `signin_rate_limit_sweeper`) emit no failure/error counters, only success counters or bare `tracing::warn!`/`error!` logging. None of their metrics were included in the Grafana dashboard/alert rules built for finding #29 the same day. A stalled or persistently-failing sweeper is invisible to the dashboard and fires no alert — log-grep-only. | `crates/embyr-server/src/sweepers/cap_usage_refresher.rs:112,137,164,180` (zero metrics at all); `soft_delete_purge_sweeper.rs:127`, `transaction_sweeper.rs` (success-only counters) | **Medium** | **CLOSED** 2026-09-19 — added shared `embyr_sweeper_error_total{sweeper=...}` counter (all 9 error branches across the 4 sweepers), new Grafana panel + `EmbyrSweeperFailingRepeatedly` alert rule, and a closed-pool failure test on `soft_delete_purge_sweeper`. |
| 48 | Database | `provision.rs`'s `Stale`/`NotPrepped`-reprovision path still never calls `backfill_collection_id()` — confirmed still genuinely open, not stale documentation. `migrate()` itself correctly adds the `collection_id` column/indexes (no correctness risk, the hybrid predicate falls back correctly for NULL rows), but pre-existing rows on a reprovisioned project stay permanently un-backfilled until a DBA manually runs `embyr-db-prep`. Matches finding #17's own documented Follow-Up Work item exactly — promoted to its own numbered finding here since it was never independently tracked. | `crates/embyr-server/src/admin/handlers/provision.rs` (direct_pg branch, ~line 400s); `crates/embyr-pg-storage/src/backend_adapter.rs:96-106` (`migrate()`); `crates/embyr-db-prep/src/main.rs:118-135` (the only current caller of the backfill) | **Medium** | **CLOSED** 2026-09-19 — `provision.rs`'s `Stale`/`NotPrepped` branch now spawns `backfill_collection_id()` (fire-and-forget `tokio::spawn`, same batch-size/throttle defaults as `embyr-db-prep`) right after `migrate()` succeeds, matching `composite_index_builder`'s own precedent for backgrounding slow admin-triggered Postgres work off the HTTP response path |
| 49 | Security | Finding #14's own fix left a documented residual gap that was never promoted to its own numbered finding: a `project_id` passing the trivial charset check (`^[a-z][a-z0-9-]{0,62}$`) but never actually provisioned still costs 3 Postgres round trips per request (the `INSERT` fails silently on an FK constraint, no disk-bloat, but the query cost is real and repeatable, pre-authentication). The main audit table marks #14 flatly CLOSED with no cross-reference — a reader trusting the table alone would believe this vector is fully closed. | `crates/embyr-server/src/middleware/rate_limit.rs:237-306` (`check_pg`); `crates/embyr-server/src/grpc/handler.rs:111-131` (`extract_project_id` validates charset, not existence); documented as `OQ-PDA-02` in `docs/evolution/2026-09-13-preauth-db-amplification.md`'s own Follow-Up section | **Medium** | **CLOSED (tracking-only)** 2026-09-19 — cross-referenced from #14's own row above so the audit table no longer misrepresents this as fully closed. No code change made: the underlying residual is unchanged from its original 2026-09-13 assessment (repeatable 3x query cost, no disk-bloat, no auth-bypass) and a real fix (existence-check cache/bloom filter, per `OQ-PDA-02`'s own suggestion) remains a legitimate future feature-sized decision, not a quick patch — deferring it again deliberately rather than rushing a design under this batch's own time pressure |
| 50 | Rust | A hard `clippy::approx_constant` `#[deny]`-level failure (two test fixtures use the literal `3.14`, which clippy reads as an imprecise `f64::consts::PI`) blocks the `cargo clippy --tests` CI gate on `embyr-server` — a different location than the already-CLOSED finding #36's own clippy-deny fix, which never touched these files. | `tests/acceptance/us_03_read_document.rs:259,388` | **Medium** | **CLOSED** 2026-09-19 — value isn't semantically pi, swapped fixture literal `3.14` -> `3.15` at both sites (no `#[allow]` needed, unlike #36's cross-crate case) |
| 51 | DevOps | `README.md` (created for finding #28 the same day) still lists the now-deleted `embyr-admin` crate as a workspace member and says "5-crate Cargo workspace" while listing 8 bullets — the bloat-pass deletion (commit `7e0d848`) landed after the README was authored and never circled back to update it. | `README.md:14` | **Medium** | **CLOSED** 2026-09-19 — removed stale `embyr-admin` bullet, corrected header to "7-crate" to match remaining bullets |
| 52 | DevOps | No resource sizing (CPU/memory request/limit) guidance exists anywhere for production `embyr-server`/`embyr-agent` containers — only DB connection pool sizing (ADR-079) is documented. Notable given this session's own 8GB dev-machine resource struggles. | `docs/operations/` (absence); `docs/product/architecture/adr-079-pool-sizing-and-limits.md` (the one adjacent thing that does exist) | **Medium** | **CLOSED** 2026-09-19 — see [`docs/operations/resource-sizing.md`](../operations/resource-sizing.md) and [`docs/evolution/2026-09-19-resource-sizing-network-topology.md`](../evolution/2026-09-19-resource-sizing-network-topology.md) |
| 53 | DevOps | DNS/load-balancer topology is architectural intent only (ADR-001 states :8080/:8081 should be externally-visible and :9090 "must be unreachable from the public network," and that BrowserChannel needs sticky routing) — nothing translates this into actual DNS names, LB target-group config, or concrete "how do I firewall :9090" steps for an operator. | `docs/product/architecture/adr-001-process-topology.md` | **Medium** | **CLOSED** 2026-09-19 — see [`docs/operations/network-topology.md`](../operations/network-topology.md) and [`docs/evolution/2026-09-19-resource-sizing-network-topology.md`](../evolution/2026-09-19-resource-sizing-network-topology.md) |
| 54 | Rust | Two small clippy nits introduced by the same day's own fixes: 8x `clippy::redundant_field_names` from the `EvalContext` refactor (finding #40) not using field-init shorthand; 1x `clippy::identity_op` from `const MAX_ADMIN_BODY_BYTES: usize = 1 * 1024 * 1024;` (finding #24), the `1 *` being a no-op. Purely stylistic, zero correctness risk. | `crates/embyr-server/src/grpc/handler.rs:957,1467,1557,1942,2017,2152,2216,2668`; `crates/embyr-server/src/admin/router.rs:95` | **Low** | Not started |
| 55 | DevOps | `deny.toml`'s `[bans]` wrapper allowlists for `tokio` and `axum` still list `"embyr-admin"` as a permitted wrapper crate, even though the whole crate was deleted the same day (bloat-pass row 1). Harmless to `cargo deny check` (a no-op entry for a nonexistent package) but is dangling config the deletion pass should have swept. | `deny.toml:68,117` | **Low** | Not started |
| 56 | Reliability | `signin_rate_limit.rs`'s Postgres-error fallback (mirrors finding #20's own fix pattern) correctly falls open to the in-process bucket on a genuine DB error, but has no `embyr_signin_rate_limit_pg_error_total` counter mirroring the main rate limiter's `embyr_rate_limit_pg_error_total` (which IS tracked in finding #29's dashboard) — only a timeout counter exists. Same observability blind spot as #47, narrower scope. | `crates/embyr-server/src/middleware/signin_rate_limit.rs:68-72` | **Low** | Not started |
| 57 | Reliability | Finding #26 (field-path-defense-in-depth, same day) introduces `panic!()` into the hottest customer-facing query path (`run_query`/`run_aggregation_query` cursor and filter code) — a deliberate, documented tradeoff matching a pre-existing local convention, and today's only caller is already validated upstream, but it converts a hypothetical future caller bug from a clean per-request error into a task panic that (via HTTP/2 connection multiplexing) can take down every in-flight stream sharing that connection, not just the offending request. Also runs counter to this project's own CLAUDE.md paradigm ("explicit `Result<T, E>` error types"). Not a bug — a design-tension note worth remembering before more panics get added under the same convention. | `crates/embyr-pg-storage/src/backend_adapter.rs` (cursor blocks, aggregation arms); `crates/embyr-pg-storage/src/encoding/query.rs` (`append_field_filter`, `order_by_expr*`) | **Low** | Not started |

`Blockers: 2 (same root cause). High: 3. Medium: 7. Low: 4. Total: 16 new items.`

## Notes

- This scan is a fresh pass, independent of `known-gaps.md` (which is fully closed — all 8 items resolved 2026-09-06 through 2026-09-08). No overlap between the two lists.
- Findings #4/#15/#16/#30 each surfaced independently from 2+ agents auditing different domains — cross-corroboration, not the same finding double-counted; kept as separate rows since each agent's own angle/evidence differs, but noted as such.
- Overall verdict from the scan: architecture is sound (hexagonal boundaries hold, admin authz is tight, CEL sandbox has no escape, TLS/secrets-rotation wiring is correct). Problems cluster in 3 places: pre-auth resource exhaustion (webhook body, metrics cardinality, rate-limiter-before-auth), features that silently no-op when an env var is unset (empty webhook secret), and things that look done but aren't wired to the real thing (composite indexes, purge sweeper, agent's field-path guard, listener reconnect). None require a redesign — most fixes are single-function.
