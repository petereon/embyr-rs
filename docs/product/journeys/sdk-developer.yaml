version: "1.0"
feature: embyr-rs
persona: P1
persona_name: Alex (SDK Developer)
jobs:
  - JOB-01  # sdk-compat
  - JOB-03  # live-sync
  - JOB-16  # client-identity-verification (added by client-auth DISCUSS, 2026-08-16)
  - JOB-17  # document-access-control (added by security-rules DISCUSS, 2026-08-17)
  - JOB-18  # embyr-hosted-identity (added by client-auth-hosted-identity DISCUSS, 2026-08-27)
  - JOB-19  # oauth-provider-identity (added by oauth-providers DISCUSS, 2026-08-30)
  - JOB-20  # anonymous-identity (added by anonymous-sessions DISCUSS, 2026-08-30)
updated: "2026-09-01"
source: docs/feature/embyr-rs/discuss/journey-sdk-developer.yaml
# NOTE (security-rules-cel-path-matching DISCUSS, 2026-09-01): JOB-17 now
# also covers importing fixed-depth multi-segment/nested-collection `.rules`
# file path patterns (e.g. `match /expeditions/{expeditionId}/
# journal_entries/{entryId} { ... }`), with deterministic routing of a
# concrete request path to the one structurally-matching pattern, across
# read, write, and Listen's per-event re-check — same job, same persona, not
# a new job (9th realization, following security-rules-cel-parity's own
# 8th). No new Alex-facing mental model beyond "the same real file, now
# covering nested/shared collections too" — Decision 3 (UX Research Depth) =
# Comprehensive for this feature (Alex's own import arc AND a new
# routing-correctness consequence arc — does a rule for one expedition ever
# leak into another? — were both mapped in full), but per this codebase's
# established convention it lives inline in
# docs/feature/security-rules-cel-path-matching/feature-delta.md § Journey,
# not as a separate journey-*.yaml artifact. This feature narrows
# security-rules-cel-parity's own deferred "Epic 4b" bucket into two: this
# feature (fixed-depth multi-segment patterns) and a new named, deferred
# follow-up, security-rules-cel-recursive-wildcards ("Epic 4b-ii",
# `{path=**}`). Confirmed by direct code read that
# security-rules-collection-group-rules' own mechanism does not reduce this
# feature's own scope — a related but orthogonal, non-reusable solution to
# an orthogonal problem. See
# docs/feature/security-rules-cel-path-matching/feature-delta.md § Job
# Discovery Framing Resolution and § Scope Assessment.
# NOTE (security-rules-cel-parity DISCUSS, 2026-09-01): JOB-17 now also covers
# importing a real, existing Firestore `.rules` file directly (the outer
# service/match-block syntax, plus a single leaf-level path-variable capture
# per match block, e.g. `match /profiles/{userId} { allow read, write: if
# request.auth.uid == userId; }`) — same job, same persona, not a new job. No
# new Alex-facing mental model beyond "the real file I already have is now the
# source of truth, instead of something I hand-retype into the JSON API" —
# Decision 3 (UX Research Depth) = Comprehensive for this feature (both Alex's
# import arc and Maria/Dana's downstream consequence arc were mapped in full),
# but per this codebase's established convention it lives inline in
# docs/feature/security-rules-cel-parity/feature-delta.md § Journey, not as a
# separate journey-*.yaml artifact. This feature REVERSES security-rules' own
# original Resolution 1 (full Firestore Rules Language parity, rejected
# 2026-08-17) and security-rules-operations' own re-confirmation of it
# (2026-08-27) — new, external customer-migration evidence, not internal
# codebase-usage evidence. The reversed full-parity target is split into 5
# independently-shippable features; this journey note covers only the first
# (narrowest, lowest-risk) slice — see
# docs/feature/security-rules-cel-parity/feature-delta.md § Changed
# Assumptions and § Scope Assessment.
# NOTE (firestore-write-streaming DISCUSS, 2026-08-30): JOB-01 now also
# covers the SDK's own default offline-write-durability behavior (the
# internal Write bidi-stream, used for offline-queue flushing / write-ahead
# durability) via this feature — same job, same persona, not a new job.
# No new Alex-facing mental model at all: Alex never calls Write directly,
# he calls setDoc()/updateDoc()/deleteDoc() exactly as before, and the SDK's
# own internal logic decides when to route over this stream instead of
# one-shot Commit — Decision 3 (UX Research Depth) = Lightweight; no
# separate journey-*.yaml artifact, journey detail lives inline in
# docs/feature/firestore-write-streaming/feature-delta.md § Journey. Two
# items are explicitly escalated to DESIGN, not decided here: the exact
# stream_token mismatch/expiry rejection mechanism, and confirmation that
# deferring backend_mode=agent Write is acceptable. See
# docs/feature/firestore-write-streaming/feature-delta.md § Job Discovery
# Framing Resolution and § Handoff Package.
# NOTE (anonymous-sessions DISCUSS, 2026-08-30): JOB-20 is a new, distinct
# job for a fourth end-user-identity-establishment mechanism (same persona,
# P1 Alex; a materially different mental model — zero prior credential of any
# kind, not a custom token, not a password, not an OAuth consent screen).
# Decision 3 (UX Research Depth) = Lightweight for this feature — backend/
# SDK-facing, not end-user UI; journey detail lives inline in
# docs/feature/anonymous-sessions/feature-delta.md § Journey, not as a
# separate journey-*.yaml artifact. Two genuinely unresolved judgment calls
# are explicitly escalated, not decided here: whether an anonymous identity's
# stability across token expiry needs a refresh-token mechanism (none exists
# anywhere in this codebase today), and whether the same backend_mode=agent
# gating JOB-18 required applies here too — the SAME open question JOB-19's
# own DISCUSS already left unresolved for itself. See
# docs/feature/anonymous-sessions/feature-delta.md § Job Discovery Framing
# Resolution and § Handoff Package.
# NOTE (batch-get-documents DISCUSS, 2026-08-30): JOB-01 now also covers
# server-side batch document resolution (db.getAll(...)) via this feature —
# same job, same persona, not a new job. No new Alex-facing mental model
# beyond "the same GetDocument he already knows, asking for several document
# paths at once instead of one" — Decision 3 (UX Research Depth) = Lightweight;
# no separate journey-*.yaml artifact, journey detail lives inline in
# docs/feature/batch-get-documents/feature-delta.md § Journey. No open
# questions escalated to DESIGN — unlike aggregation-queries (OQ-AGG-01),
# every judgment call was resolvable directly from this codebase's own
# already-shipped GetDocument precedent, confirmed by direct reading, not
# guessed. See docs/feature/batch-get-documents/feature-delta.md § Job
# Discovery Framing Resolution.
# NOTE (aggregation-queries DISCUSS, 2026-08-30): JOB-01 now also covers
# server-side count()/sum()/average() aggregation queries via this feature —
# same job, same persona, not a new job. No new Alex-facing mental model
# beyond "the same filtered-query builder he already knows, asking for a
# computed summary instead of documents" — Decision 3 (UX Research Depth) =
# Lightweight; no separate journey-*.yaml artifact, journey detail lives
# inline in docs/feature/aggregation-queries/feature-delta.md § Journey.
# One item is explicitly escalated to DESIGN, not decided here: whether real
# Firestore's own RunAggregationQuery wire method is unary or
# server-streaming (OQ-AGG-01, no verification tool available to this
# DISCUSS). See docs/feature/aggregation-queries/feature-delta.md § Job
# Discovery Framing Resolution and § Handoff Package.
# NOTE (oauth-providers DISCUSS, 2026-08-30): JOB-19 is a new, distinct job
# for one-click "Sign in with Google" (same persona, P1 Alex; a THIRD mental
# model distinct from both JOB-16's custom-token bridge and JOB-18's hosted
# email/password — a third-party OAuth provider's own consent screen
# performs authentication). Decision 3 (UX Research Depth) = Comprehensive
# for this feature — Maria's one-click-but-first-time-consent emotional arc
# was mapped in full, but per this codebase's established convention it
# lives inline in docs/feature/oauth-providers/feature-delta.md § Journey,
# not as a separate journey-*.yaml artifact. v1 scope is Google only
# (Resolution 1) — GitHub and other non-OIDC providers are an explicitly
# named, deferred candidate follow-up, not silently ruled out. One genuinely
# unresolved, security-adjacent judgment call is explicitly escalated: does
# this job's verification mechanism need the same backend_mode=agent gating
# JOB-18 required (see feature-delta.md § Job Discovery Framing Resolution,
# Resolution 2).
# NOTE (client-auth-hosted-identity DISCUSS, 2026-08-27): JOB-18 is a new,
# distinct job for embyr-hosted email/password identity (same persona, P1
# Alex; goal differs from JOB-16's custom-token-verification focus — Alex's
# end users create an account DIRECTLY with embyr, no backend of Trailmark's
# own in the loop). Decision 3 (UX Research Depth) = Comprehensive for this
# feature — unlike every prior Authorization epic's Lightweight system-flow
# journey, Maria's own signup/signin/reset emotional arc was mapped in full
# (anxious about another account → relieved it was simple; frustrated/locked
# out → relieved on reset completion) — but per this codebase's established
# convention (mirrors security-rules' own precedent for Comprehensive depth),
# it still lives inline in
# docs/feature/client-auth-hosted-identity/feature-delta.md § Journey, not as
# a separate journey-*.yaml artifact. Two judgment calls remain genuinely
# unresolved and explicitly escalated, not decided here: hosted-identity
# credential storage location / VPC-egress fit for JOB-04 (Riley)-class
# customers, and exact bounded-context placement for the new hosted
# end-user Account entity.
# NOTE (security-rules-operations DISCUSS, 2026-08-27): JOB-17 now also
# covers rule history, versioning, rollback, and change attribution across
# all 3 rule tables (access_rules, write_access_rules, group_access_rules)
# via this feature — same job, same persona, not a new job. No new
# Alex-facing mental model beyond "the existing define/redefine action now
# also remembers every value a rule has ever held, attributed to whoever set
# it" — Decision 3 (UX Research Depth) = Lightweight; no separate
# journey-*.yaml artifact. This feature also confirmed, by exhaustive
# evidence across all 6 prior epics, that "richer condition grammar" has no
# firing trigger and remains out of scope, unbuilt. See
# docs/feature/security-rules-operations/feature-delta.md § Journey.
# NOTE (custom-claims DISCUSS, 2026-08-26): JOB-17 now also covers
# role/attribute-based (custom-claims) condition grammar via this feature —
# same job, same persona, not a new job. Genuinely cross-cutting: this
# feature also amends JOB-16's own token contract (ADR-024) to carry an
# arbitrary claims map embedded by the customer's backend at mint time. No
# new Alex-facing mental model beyond "a claim is something my own backend
# already embeds in the token I already mint" — Decision 3 (UX Research
# Depth) = Lightweight; no separate journey-*.yaml artifact, per the
# established pattern for a pure grammar/mechanism extension. See
# docs/feature/custom-claims/feature-delta.md § Journey.
# NOTE (security-rules-realtime DISCUSS, 2026-08-26): JOB-17 now also
# covers real-time Listen enforcement AND correctly-scoped real-time
# delivery via this feature — same job, same persona, not a new job.
# Investigation found Listen's own delivery mechanism was, independent of
# authorization, silently ignoring the client's own query filter on the
# initial snapshot and leaking every collection's live changes to every
# subscriber project-wide; both fixed as a necessary foundation for
# collection-scoped enforcement, confirmed in-scope with the orchestrator.
# See docs/feature/security-rules-realtime/feature-delta.md § Job Discovery
# Framing Resolution.
# NOTE (security-rules-collection-group-rules DISCUSS, 2026-08-25): JOB-17
# now also covers collection-group (collectionGroup()/all_descendants=true)
# query enforcement via this feature — same job, same persona, not a new
# job. Decision 3 (UX Research Depth) = Lightweight for this feature; only a
# short delta (a new, independently-authored, independently-stored
# collection-group rule, disjoint from the existing exact-path rule) is
# added, inline in
# docs/feature/security-rules-collection-group-rules/feature-delta.md
# § Journey, not as a separate journey-*.yaml artifact.
# NOTE (security-rules-query-path DISCUSS, 2026-08-18): JOB-17 now also
# covers query-path (RunQuery) enforcement via this feature — same job, same
# persona, not a new job (see docs/product/jobs.yaml JOB-17 NOTE). Decision 3
# (UX Research Depth) = Lightweight for this feature — Alex's authoring
# mental model and Maria/Dana's stakes are already comprehensively
# established in security-rules' own journey; only a short delta (query-
# shape compliance replacing the "filter after fetch" antipattern) is added,
# inline in docs/feature/security-rules-query-path/feature-delta.md
# § Journey, not as a separate journey-*.yaml artifact.
# NOTE (client-auth DISCUSS, 2026-08-16): JOB-16 is a new, distinct job for
# per-end-user identity verification (P1 Alex is still the persona who
# "hires" the capability, but the goal differs from JOB-01/JOB-03's Firestore
# data-plane focus). No separate visual/YAML journey artifact was produced
# for JOB-16 — Decision 3 (UX Research Depth) = Lightweight for this feature;
# journey detail lives inline in docs/feature/client-auth/feature-delta.md
# per the current single-narrative-file convention.
# NOTE (security-rules DISCUSS, 2026-08-17): JOB-17 is a new, distinct job
# for per-collection access-control rules (same persona, P1 Alex; goal is
# authoring/enforcing authorization, distinct from JOB-16's identity
# establishment). Decision 3 (UX Research Depth) = Comprehensive for this
# feature — full emotional-arc journey work (Alex authoring/testing AND the
# downstream consequence for Trailmark's end users, e.g. Maria Santos) was
# produced, but still lives inline in
# docs/feature/security-rules/feature-delta.md per the single-narrative-file
# convention, not as a separate journey-*.yaml artifact. v1 scope (this
# feature) covers read-path (GetDocument) enforcement only.
# NOTE (security-rules-write-path DISCUSS, 2026-08-18): JOB-17 now also
# covers write-path (CreateDocument/UpdateDocument/DeleteDocument)
# enforcement via this feature — same job, same persona, not a new job (see
# docs/product/jobs.yaml JOB-17 NOTE). Decision 3 (UX Research Depth) =
# Lightweight for this feature — Alex's authoring mental model and Maria/
# Dana's stakes are already comprehensively established in security-rules'
# own journey; only a short delta (the resource/request.resource
# distinction) is added, inline in
# docs/feature/security-rules-write-path/feature-delta.md § Journey, not as
# a separate journey-*.yaml artifact.
