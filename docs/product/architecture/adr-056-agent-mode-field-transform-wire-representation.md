# ADR-056: Agent-Mode Field Transform Wire Representation — RENUMBERED

This ADR number collided with a concurrent sibling DESIGN wave
(`adr-056-agent-transaction-sweeper-purge-sql-and-retention-config.md`,
claimed first). Per this session's own standing instruction, the content
originally drafted here was renumbered to
**`adr-057-agent-mode-field-transform-wire-representation.md`**.

This stub is left in place only so the filename collision is discoverable;
it carries no decision content of its own. See ADR-057 for the actual ADR.
