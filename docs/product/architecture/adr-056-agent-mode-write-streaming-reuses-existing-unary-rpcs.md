# Superseded — renumbered to ADR-060

This file's number (`adr-056`) collided with two concurrently-running
sibling DESIGN waves this session (`agent-mode-list-collection-ids`,
`agent-mode-field-transforms`), both of which also drafted an ADR at `056`
before their own renumbering. This feature's own content moved to:

`docs/product/architecture/adr-060-agent-mode-write-streaming-reuses-existing-unary-rpcs.md`

No tool available to this agent can delete a file; this stub replaces the
original content so this path does not read as a second, conflicting
canonical ADR-056.
