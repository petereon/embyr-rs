--- crates/embyr-core/src/auth/blake3.rs
+++ replace derive_cache_key -> [u8; 32] with [1; 32]
@@ -1,10 +1,10 @@
 pub fn derive_cache_key(api_key: &[u8]) -> [u8; 32] {
-    *blake3::hash(api_key).as_bytes()
+    [1; 32] /* ~ changed by cargo-mutants ~ */
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
 
     #[test]
     fn blake3_cache_key_is_deterministic() {
