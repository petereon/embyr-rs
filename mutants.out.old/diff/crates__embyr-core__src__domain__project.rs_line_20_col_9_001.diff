--- crates/embyr-core/src/domain/project.rs
+++ replace ProjectId::as_str -> &str with "xyzzy"
@@ -12,17 +12,17 @@
         } else {
             Err(CoreError::InvalidArgument(format!(
                 "project id must match ^[a-z][a-z0-9-]{{0,62}}$, got: {s}"
             )))
         }
     }
 
     pub fn as_str(&self) -> &str {
-        &self.0
+        "xyzzy" /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
 
     // --- ProjectId::as_str ---
